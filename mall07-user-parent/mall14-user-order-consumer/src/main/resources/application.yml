server:
  port: 6000

spring:
  application:
    name: myteam-mall-order
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  redis:
    host: 127.0.0.1
  session:
    store-type: redis

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka

ribbon:
  ReadTimeout: 60000
  ConnectTimeout: 60000

